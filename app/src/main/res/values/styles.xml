<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="ClickableOption" parent="TextAppearance.AppCompat.Subhead">
        <!-- Customize your theme here. -->
        <item name="android:textSize">18sp</item>
    </style>

    <style name="LinkStyle" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center</item>
        <item name="android:drawablePadding">@dimen/keyline_1</item>
        <item name="android:paddingLeft">@dimen/keyline_1</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">@dimen/keyline_1</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:drawableTint">@color/colorLink</item>
        <item name="android:textColor">@color/colorLink</item>
    </style>

    <style name="appTitle" parent="TextAppearance.AppCompat.Subhead">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/colorPrimaryLight</item>
        <item name="android:padding">@dimen/space_between_content_areas</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
